## 1. Shared rules

- [x] 1.1 Update canonical rules and indexes; verify emulator tests for both authenticated identities, unauthenticated denial, descendants, profiles, and legacy paths.
- [x] 1.2 Publish only the tested Firestore rules after migration and verify the published source matches the local rules.

## 2. Validation

- [x] 2.1 Document shared paths and rule commands; verify strict specs and npm run check without e2e.
