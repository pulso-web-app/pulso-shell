# shell-feature

`shell-feature` owns the authenticated application frame, navigation, and layout presented around remote product areas. It consumes authentication state but does not implement product-domain behavior.

Public imports use `@pulso-shell/shell-feature`.

Run `npx nx test shell-feature` for focused tests.
