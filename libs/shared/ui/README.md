# Shared UI

`shared-ui` contains shell-local, domain-neutral presentation primitives such as loading feedback and decorative particles. Feature behavior, authentication state, and remote-specific UI do not belong here.

Public imports use `@pulso-shell/shared-ui`.

Run `npx nx test shared-ui` for focused tests.
